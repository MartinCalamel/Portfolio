# This is a basic workflow to help you get started with Actions

name: syntaxe

on:
  pull_request:
    branches: 
      - "main"
      - "Dev"

  workflow_dispatch:
jobs:
  syntaxe:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Installation de l'environnement
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Installation des dépendances
        run: npm install --save-dev eslint stylelint htmlhint

      
      - name: Vérification JS avec ESLint
        run: eslint -c .github/configs/.eslintrc.json "site/scripts/*.js"

      - name: Vérification CSS avec Stylelint
        run: stylelint --config .github/configs/.stylelintrc.json "site/style/*.css"

      - name: Vérification HTML avec HTMLHint
        run:  htmlhint -c .github/configs/.htmlhintrc "site/html/*.html"
      
      
                
